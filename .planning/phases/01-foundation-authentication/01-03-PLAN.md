---
phase: 01-foundation-authentication
plan: 03
type: execute
wave: 3
depends_on: [01-foundation-authentication-02]
files_modified: [src/main/java/com/shop/service/AuthService.java, src/main/java/com/shop/controller/AuthController.java, src/main/java/com/shop/entity/RefreshToken.java, src/main/java/com/shop/repository/RefreshTokenRepository.java, src/main/java/com/shop/controller/UserController.java, src/main/java/com/shop/service/UserService.java]
autonomous: true
requirements: [AUTH-02, AUTH-03, USER-02]
must_haves:
  truths:
    - "User can refresh an expired access token using a valid refresh token via POST /api/auth/refresh"
    - "Refresh token rotation is implemented (old token is invalidated/replaced on use)"
    - "User can fetch their own account details via GET /api/users/me"
    - "User can update their profile information via PUT /api/users/profile"
  artifacts:
    - path: "src/main/java/com/shop/entity/RefreshToken.java"
      provides: "Entity for storing refresh tokens in the database"
    - path: "src/main/java/com/shop/controller/UserController.java"
      provides: "Endpoints for authenticated user operations"
    - path: "src/main/java/com/shop/service/UserService.java"
      provides: "Logic for profile retrieval and update"
  key_links:
    - from: "AuthService.refresh"
      to: "RefreshTokenRepository"
      via: "Database lookup and deletion/update"
    - from: "UserController"
      to: "SecurityContextHolder"
      via: "Retrieval of current authenticated user ID/email"
---

<objective>
Complete the authentication flow with database-backed refresh token rotation and implement user profile management APIs.

Purpose: To provide a complete and secure identity lifecycle, including session persistence and profile updates.
Output: Functional token refresh flow and secured profile endpoints.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/1-CONTEXT.md
@.planning/phases/01-foundation-authentication/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh Token Database Storage & Rotation (AUTH-02)</name>
  <files>src/main/java/com/shop/entity/RefreshToken.java, src/main/java/com/shop/repository/RefreshTokenRepository.java, src/main/java/com/shop/service/AuthService.java, src/main/java/com/shop/controller/AuthController.java</files>
  <action>
    - Create `RefreshToken` entity and repository.
    - Update `AuthService.login` to generate and save a Refresh Token in the database.
    - Implement `AuthService.refresh`:
        - Validate the provided refresh token against the database.
        - Check for expiration.
        - Generate new Access Token (30s).
        - Rotate Refresh Token (delete old, save new).
    - Add `POST /api/auth/refresh` endpoint to `AuthController`.
  </action>
  <verify>
    <automated>mvn test -Dtest=AuthRefreshIntegrationTest</automated>
  </verify>
  <done>Tokens can be refreshed using the DB-backed rotation strategy.</done>
</task>

<task type="auto">
  <name>Task 2: User Profile Management (AUTH-03, USER-02)</name>
  <files>src/main/java/com/shop/service/UserService.java, src/main/java/com/shop/controller/UserController.java, src/main/java/com/shop/dto/UserProfileResponse.java, src/main/java/com/shop/dto/UpdateProfileRequest.java</files>
  <action>
    - Implement `UserService`:
        - `getCurrentUser`: Get user details by email/ID.
        - `updateProfile`: Update name, phone, address, etc.
    - Create `UserController` (Secured):
        - `GET /api/users/me` (AUTH-03)
        - `PUT /api/users/profile` (USER-02)
    - Ensure only the authenticated user can access/update their own data.
  </action>
  <verify>
    <automated>mvn test -Dtest=UserControllerTest</automated>
  </verify>
  <done>Users can view and manage their profile information securely.</done>
</task>

<task type="auto">
  <name>Task 3: Phase Verification & Integration Testing</name>
  <files>src/test/java/com/shop/AuthIntegrationTest.java</files>
  <action>
    - Write a comprehensive integration test that:
        1. Signs up a user.
        2. Logs in to get tokens.
        3. Accesses a secured endpoint.
        4. Waits for Access Token to expire (31s).
        5. Refreshes the token.
        6. Accesses the secured endpoint again with the new token.
        7. Updates the profile.
  </action>
  <verify>
    <automated>mvn test -Dtest=AuthIntegrationTest</automated>
  </verify>
  <done>The entire Phase 1 authentication and user flow is verified.</done>
</task>

</tasks>

<verification>
- Access token expires in 30s.
- Refresh token rotation works (old refresh token cannot be used again).
- /api/users/me returns the correct user profile.
- /api/users/profile updates the user data in the database.
</verification>

<success_criteria>
All Phase 1 requirements (AUTH-01, AUTH-02, AUTH-03, USER-02) are met, verified with automated integration tests, and follow the technical constraints (Virtual Threads, DB-stored Refresh Tokens, 30s Access Expiry).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-03-SUMMARY.md`
</output>
