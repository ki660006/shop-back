---
phase: 01-foundation-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pom.xml, src/main/resources/application.yml, src/main/java/com/shop/ShopBackApplication.java, src/main/resources/db/migration/V1__init_schema.sql, src/main/java/com/shop/dto/ErrorResponse.java, src/main/java/com/shop/exception/GlobalExceptionHandler.java]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, USER-02]
must_haves:
  truths:
    - "Application starts with Java 21 Virtual Threads enabled (log verification or config check)"
    - "Database tables (users, refresh_tokens) are successfully created by Flyway on startup"
    - "Invalid API requests return the standardized JSON error response format"
  artifacts:
    - path: "pom.xml"
      provides: "Maven build configuration with Spring Boot 3.4.3 and Java 21"
    - path: "src/main/resources/application.yml"
      provides: "Database connectivity and virtual thread activation (spring.threads.virtual.enabled=true)"
    - path: "src/main/resources/db/migration/V1__init_schema.sql"
      provides: "Initial PostgreSQL schema for users and refresh_tokens"
    - path: "src/main/java/com/shop/exception/GlobalExceptionHandler.java"
      provides: "Standardized error handling for all REST controllers"
  key_links:
    - from: "application.yml"
      to: "PostgreSQL 16"
      via: "JDBC URL and credentials"
    - from: "GlobalExceptionHandler"
      to: "ErrorResponse DTO"
      via: "Jackson JSON mapping"
---

<objective>
Initialize the Spring Boot Maven project with the necessary technical foundation for high-performance I/O and robust error handling.

Purpose: To ensure the project starts with a scalable architecture (Virtual Threads) and standardized data structures (Flyway/Error DTOs).
Output: A running Spring Boot application connected to PostgreSQL 16 with an initial schema.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/1-CONTEXT.md
@.planning/phases/01-foundation-authentication/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Maven Project & Virtual Threads</name>
  <files>pom.xml, src/main/resources/application.yml, src/main/java/com/shop/ShopBackApplication.java</files>
  <action>
    - Create `pom.xml` with:
        - Parent: `spring-boot-starter-parent` (3.4.3)
        - Dependencies: `spring-boot-starter-web`, `spring-boot-starter-security`, `spring-boot-starter-data-jpa`, `postgresql`, `flyway-core`, `flyway-database-postgresql`, `lombok`, `mapstruct`, `jjwt-api` (0.13.0), `jjwt-impl`, `jjwt-jackson`.
        - Java version: 21.
    - Create `src/main/resources/application.yml` with:
        - `spring.threads.virtual.enabled: true` per D-01.
        - PostgreSQL datasource configuration.
        - Hibernate ddl-auto: `none` (as we use Flyway).
    - Create the main application class `ShopBackApplication.java`.
  </action>
  <verify>
    <automated>mvn compile && grep "virtual.enabled: true" src/main/resources/application.yml</automated>
  </verify>
  <done>Project compiles and virtual threads are configured.</done>
</task>

<task type="auto">
  <name>Task 2: Database Connectivity & Initial Flyway Migration</name>
  <files>src/main/resources/db/migration/V1__init_schema.sql</files>
  <action>
    - Create `V1__init_schema.sql` defining:
        - `users` table: id (bigint/serial), email (unique), password, name, nickname, phone, address, role, timestamps.
        - `refresh_tokens` table: id, user_id (fk), token, expiry_date.
    - Ensure PostgreSQL 16 is accessible based on `application.yml` settings.
  </action>
  <verify>
    <automated>mvn spring-boot:run -Dspring-boot.run.arguments="--server.port=0" --quiet & (sleep 10; kill $!) && echo "Check if tables exist in DB"</automated>
  </verify>
  <done>Flyway migration completes successfully on startup, creating the schema.</done>
</task>

<task type="auto">
  <name>Task 3: Standardized Error Response Implementation</name>
  <files>src/main/java/com/shop/dto/ErrorResponse.java, src/main/java/com/shop/exception/GlobalExceptionHandler.java</files>
  <action>
    - Create `ErrorResponse` record/DTO with fields: `code`, `message`, `timestamp`, `details`.
    - Implement `GlobalExceptionHandler` using `@RestControllerAdvice` to catch:
        - `Exception` (catch-all for 500)
        - `MethodArgumentNotValidException` (for validation errors)
        - `ResponseStatusException`
    - Ensure it returns the format specified in CONTEXT.md.
  </action>
  <verify>
    <automated>mvn test -Dtest=GlobalExceptionHandlerTest</automated>
  </verify>
  <done>Any unhandled or specific exception results in the standardized error JSON.</done>
</task>

</tasks>

<verification>
- Application starts without errors.
- `spring.threads.virtual.enabled` is true.
- DB tables `users` and `refresh_tokens` are present.
- 404 response follows the specified ErrorResponse structure.
</verification>

<success_criteria>
A stable backend foundation with Java 21, PostgreSQL connectivity, Flyway versioning, and standardized error messaging is ready for authentication implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-01-SUMMARY.md`
</output>
