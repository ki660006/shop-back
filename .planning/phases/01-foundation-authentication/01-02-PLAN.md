---
phase: 01-foundation-authentication
plan: 02
type: execute
wave: 2
depends_on: [01-foundation-authentication-01]
files_modified: [src/main/java/com/shop/config/SecurityConfig.java, src/main/java/com/shop/security/JwtTokenProvider.java, src/main/java/com/shop/security/JwtAuthenticationFilter.java, src/main/java/com/shop/service/AuthService.java, src/main/java/com/shop/controller/AuthController.java, src/main/java/com/shop/repository/UserRepository.java, src/main/java/com/shop/entity/User.java]
autonomous: true
requirements: [AUTH-01, AUTH-02]
must_haves:
  truths:
    - "User can successfully sign up via POST /api/auth/signup"
    - "User can login via POST /api/auth/login and receive a JWT access token"
    - "Access token has a configured expiration of 30 seconds for verification"
    - "Requests to secured endpoints without a valid token return 401 Unauthorized"
  artifacts:
    - path: "src/main/java/com/shop/config/SecurityConfig.java"
      provides: "Stateless security configuration using Lambda DSL"
    - path: "src/main/java/com/shop/security/JwtTokenProvider.java"
      provides: "JWT creation, parsing, and validation logic"
    - path: "src/main/java/com/shop/security/JwtAuthenticationFilter.java"
      provides: "Request filter to extract and validate JWT from Authorization header"
  key_links:
    - from: "JwtAuthenticationFilter"
      to: "SecurityConfig"
      via: "addFilterBefore(UsernamePasswordAuthenticationFilter.class)"
    - from: "AuthService"
      to: "UserRepository"
      via: "Database check for login"
---

<objective>
Implement the core authentication mechanism using Spring Security and JWT, enabling users to register and obtain access tokens.

Purpose: To secure the application and establish user identity.
Output: Working Signup and Login APIs with JWT-based security enforcement.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/1-CONTEXT.md
@.planning/phases/01-foundation-authentication/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Security Configuration & JWT Provider</name>
  <files>src/main/java/com/shop/config/SecurityConfig.java, src/main/java/com/shop/security/JwtTokenProvider.java</files>
  <action>
    - Implement `JwtTokenProvider` using JJWT:
        - Generate Access Token with 30s expiration (per CONTEXT.md).
        - Secret key sourced from environment variable or default for dev.
        - Methods to validate token and get authentication from token.
    - Implement `SecurityConfig` (Spring Security 6.4+):
        - Stateless session management.
        - Disable CSRF.
        - Permit all to `/api/auth/**`.
        - Secure all other requests.
        - Register `JwtAuthenticationFilter` (to be created in Task 3).
        - Use `BCryptPasswordEncoder`.
  </action>
  <verify>
    <automated>mvn test -Dtest=JwtTokenProviderTest</automated>
  </verify>
  <done>JWT generation and validation logic is implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: User Registration & Login (AUTH-01, AUTH-02)</name>
  <files>src/main/java/com/shop/entity/User.java, src/main/java/com/shop/repository/UserRepository.java, src/main/java/com/shop/service/AuthService.java, src/main/java/com/shop/controller/AuthController.java</files>
  <action>
    - Create `User` entity with necessary fields (email, password, etc.).
    - Create `UserRepository`.
    - Implement `AuthService`:
        - `signup`: Hash password and save user.
        - `login`: Verify credentials and return JWT Access Token.
    - Create `AuthController`:
        - `POST /api/auth/signup`
        - `POST /api/auth/login`
  </action>
  <verify>
    <automated>mvn test -Dtest=AuthServiceTest</automated>
  </verify>
  <done>Users can register and login to receive tokens.</done>
</task>

<task type="auto">
  <name>Task 3: JWT Authentication Filter & Integration</name>
  <files>src/main/java/com/shop/security/JwtAuthenticationFilter.java</files>
  <action>
    - Implement `JwtAuthenticationFilter` extending `OncePerRequestFilter`:
        - Extract "Authorization: Bearer <token>" from headers.
        - Validate token using `JwtTokenProvider`.
        - Set authentication in `SecurityContextHolder`.
    - Ensure filter is correctly registered in `SecurityConfig`.
  </action>
  <verify>
    <automated>curl -v -X GET http://localhost:8080/api/users/me</automated>
  </verify>
  <done>Requests to secured endpoints are intercepted and authenticated via JWT.</done>
</task>

</tasks>

<verification>
- POST /api/auth/signup returns 201 Created.
- POST /api/auth/login returns a token.
- Token expires in 30 seconds (verifiable by waiting 31s and checking a secured endpoint).
- Accessing a non-auth endpoint without a token returns 401.
</verification>

<success_criteria>
Core authentication is functional. Users can create accounts and get valid (but short-lived) access tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-02-SUMMARY.md`
</output>
