---
phase: 02-product-catalog-discovery
plan: 02-01
type: execute
wave: 1
depends_on: ["01-03"]
files_modified: [pom.xml, src/main/java/com/shop/config/QuerydslConfig.java, src/main/java/com/shop/config/SecurityConfig.java, src/main/resources/db/migration/V2__create_catalog_schema.sql, src/main/java/com/shop/entity/Category.java, src/main/java/com/shop/repository/CategoryRepository.java, src/main/java/com/shop/service/CategoryService.java, src/main/java/com/shop/controller/CategoryController.java, src/main/java/com/shop/dto/CategoryResponse.java]
autonomous: true
requirements: [SHOP-01, CORE-01]
must_haves:
  truths:
    - "Hierarchical category tree is returned as a single JSON structure"
    - "All primary keys and foreign keys for categories are UUIDs"
    - "Category API is accessible without authentication"
  artifacts:
    - path: "src/main/resources/db/migration/V2__create_catalog_schema.sql"
      provides: "Initial schema for catalog with UUID and FTS"
    - path: "src/main/java/com/shop/service/CategoryService.java"
      provides: "Bulk load and tree reconstruction logic"
  key_links:
    - from: "src/main/java/com/shop/entity/Category.java"
      to: "UUID v7 generation"
      via: "uuid-creator library"
---

<objective>
Phase 2 foundation establishment and implementation of hierarchical category tree.

Purpose: To provide a performant base for the product catalog with hierarchical organization and modern ID standards.
Output: Catalog schema, Querydsl configuration, and working Category Tree API.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/2-CONTEXT.md
@.planning/phases/02-product-catalog-discovery/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies and basic configuration</name>
  <files>pom.xml, src/main/java/com/shop/config/QuerydslConfig.java, src/main/java/com/shop/config/SecurityConfig.java</files>
  <action>
    - Add `uuid-creator`, `querydsl-jpa (jakarta classifier)`, and `tika-core` dependencies to `pom.xml`.
    - Configure Querydsl annotation processor in `maven-compiler-plugin`.
    - Create `QuerydslConfig.java` to expose `JPAQueryFactory` as a Bean.
    - Update `SecurityConfig.java` to permit GET access to `/api/categories/**` and `/api/products/**` (per SHOP-01, SHOP-02).
  </action>
  <verify>
    <automated>mvn compile</automated>
  </verify>
  <done>Querydsl is configured and project compiles with new dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Create catalog schema with UUID and FTS</name>
  <files>src/main/resources/db/migration/V2__create_catalog_schema.sql</files>
  <action>
    - Create `categories` table with `id UUID PK`, `parent_id UUID FK (self)`, `name`, `slug (UK)`.
    - Create `products` table with `id UUID PK`, `category_id UUID FK`, `name`, `description`, `price`, `status`, `stock_quantity`, `created_at`.
    - Add `search_vector` generated column to `products` (using `name` and `description`).
    - Create GIN index on `products(search_vector)`.
    - Create `product_images` table with `id UUID PK`, `product_id UUID FK`, `image_url`, `is_primary`, `sort_order`.
    - Per D-01: Ensure all PKs and FKs use UUID type.
  </action>
  <verify>
    <automated>mvn flyway:info</automated>
  </verify>
  <done>Flyway migration file is created with correct UUID types and FTS index.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Hierarchical Category API</name>
  <files>src/main/java/com/shop/entity/Category.java, src/main/java/com/shop/repository/CategoryRepository.java, src/main/java/com/shop/service/CategoryService.java, src/main/java/com/shop/controller/CategoryController.java, src/main/java/com/shop/dto/CategoryResponse.java</files>
  <action>
    - Implement `Category` entity with UUID PK using `uuid-creator` for generation (v7).
    - Implement `CategoryService` to fetch all categories in one query and build the tree structure in memory (Bulk Load pattern per RESEARCH.md).
    - Implement `CategoryController` with `GET /api/categories` returning the hierarchical tree.
  </action>
  <verify>
    <automated>mvn test -Dtest=CategoryServiceTest</automated>
  </verify>
  <done>Categories can be retrieved as a hierarchical tree via API.</done>
</task>

</tasks>

<verification>
1. Run application and verify `GET /api/categories` returns empty list (initially).
2. Seed sample categories and verify nested structure in response.
</verification>

<success_criteria>
- Flyway V2 migration applied successfully.
- Querydsl Q-classes generated (verified by `mvn compile`).
- Category tree API returns correct hierarchy.
</success_criteria>

<output>
After completion, create `.planning/phases/02-product-catalog-discovery/02-01-SUMMARY.md`
</output>
