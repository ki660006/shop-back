---
phase: 02-product-catalog-discovery
plan: 02-03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: [src/main/java/com/shop/service/FileStorageService.java, src/main/java/com/shop/config/WebConfig.java, src/main/java/com/shop/entity/ProductImage.java, src/main/java/com/shop/repository/ProductImageRepository.java, src/main/java/com/shop/controller/ProductImageController.java, src/test/java/com/shop/CatalogIntegrationTest.java]
autonomous: true
requirements: [CORE-01]
must_haves:
  truths:
    - "Product images are stored as UUID filenames in the local filesystem"
    - "MIME type verification prevents malicious file uploads"
    - "Uploaded images are accessible via a static resource URL"
  artifacts:
    - path: "src/main/java/com/shop/service/FileStorageService.java"
      provides: "Secure file storage operations"
    - path: "src/test/java/com/shop/CatalogIntegrationTest.java"
      provides: "Full-flow verification for Phase 2"
  key_links:
    - from: "src/main/java/com/shop/config/WebConfig.java"
      to: "Local storage directory"
      via: "ResourceHandlerRegistry"
---

<objective>
Management of product media and comprehensive phase verification.

Purpose: To allow product visualization and ensure all catalog features work together correctly.
Output: Secure file upload system and cross-functional integration tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/2-CONTEXT.md
@.planning/phases/02-product-catalog-discovery/02-RESEARCH.md
@.planning/phases/02-product-catalog-discovery/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Secure File Storage Service</name>
  <files>src/main/java/com/shop/service/FileStorageService.java, src/main/java/com/shop/config/WebConfig.java</files>
  <action>
    - Implement `FileStorageService` using `uuid-creator` for filenames.
    - Implement MIME type verification using `Apache Tika` to ensure only images are uploaded.
    - Add path sanitization to prevent path traversal.
    - Configure `WebConfig` to serve files from the upload directory as static resources.
    - Use application properties to define the root upload path (e.g., `shop.upload.path`).
  </action>
  <verify>
    <automated>mvn test -Dtest=FileStorageServiceTest</automated>
  </verify>
  <done>Images can be securely stored and served via URL.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Product Image Mapping and Upload API</name>
  <files>src/main/java/com/shop/entity/ProductImage.java, src/main/java/com/shop/repository/ProductImageRepository.java, src/main/java/com/shop/controller/ProductImageController.java</files>
  <action>
    - Implement `ProductImage` entity with UUID PK and FK to `Product`.
    - Implement `POST /api/products/{productId}/images` to upload multiple images.
    - First image uploaded should be marked as `is_primary` if none exists.
    - Update `ProductDetailResponse` in `ProductService` to include image URLs.
  </action>
  <verify>
    <automated>curl -X POST -F "files=@image.png" http://localhost:8080/api/products/{id}/images</automated>
  </verify>
  <done>Images can be attached to products and retrieved in details.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 2 Comprehensive Integration Test</name>
  <files>src/test/java/com/shop/CatalogIntegrationTest.java</files>
  <action>
    - Implement a full-flow integration test using Testcontainers (PostgreSQL).
    - Scenario:
      1. Create Categories and Products.
      2. Upload images to Products.
      3. Search Products by keyword and filter by Category.
      4. Verify Cursor Paging across multiple pages.
      5. Fetch Product Detail and verify images and mock data.
  </action>
  <verify>
    <automated>mvn test -Dtest=CatalogIntegrationTest</automated>
  </verify>
  <done>All Phase 2 requirements are verified via automated integration tests.</done>
</task>

</tasks>

<verification>
1. Upload a PNG and verify it's stored and served.
2. Upload a .txt file disguised as .png and verify it's rejected by Tika.
3. Run `mvn test` to ensure all catalog integration tests pass.
</verification>

<success_criteria>
- Images are correctly mapped to products and stored with UUID names.
- Security checks (Tika, path sanitization) are functional.
- Integration tests cover Search, Filter, Paging, and Images.
</success_criteria>

<output>
After completion, create `.planning/phases/02-product-catalog-discovery/02-03-SUMMARY.md`
</output>
