---
phase: 04-checkout-order-management
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified: [
  "src/main/java/com/shop/domain/order/service/OrderService.java",
  "src/main/java/com/shop/domain/order/controller/OrderController.java",
  "src/test/java/com/shop/OrderIntegrationTest.java"
]
autonomous: true
requirements: [ORDER-02]
---

<objective>
Implement order history, detailed view, cancellation (with stock restoration), and comprehensive integration tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/4-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Order History and Details</name>
  <files>src/main/java/com/shop/domain/order/service/OrderService.java, src/main/java/com/shop/domain/order/controller/OrderController.java</files>
  <action>
    Implement `GET /api/orders` to list current user's orders (with pagination).
    Implement `GET /api/orders/{id}` for detailed order view including OrderItems.
  </action>
  <verify>
    <automated>mvn compile</automated>
  </verify>
  <done>User can view order history.</done>
</task>

<task type="auto">
  <name>Task 2: Order Cancellation</name>
  <files>src/main/java/com/shop/domain/order/service/OrderService.java, src/main/java/com/shop/domain/order/controller/OrderController.java</files>
  <action>
    Implement `POST /api/orders/{id}/cancel`:
    - Ensure order belongs to user and is in COMPLETED state.
    - Change status to CANCELLED.
    - Restore stock for all products in the order using pessimistic lock.
  </action>
  <verify>
    <automated>mvn compile</automated>
  </verify>
  <done>User can cancel orders and stock is restored.</done>
</task>

<task type="auto">
  <name>Task 3: Integration Testing</name>
  <files>src/test/java/com/shop/OrderIntegrationTest.java</files>
  <action>
    Write `OrderIntegrationTest` to verify:
    1. Cart to Order conversion.
    2. Concurrent checkout (stock depletion handling).
    3. Order history retrieval.
    4. Order cancellation and stock restoration.
  </action>
  <verify>
    <automated>mvn compile</automated>
  </verify>
  <done>Phase 4 verified.</done>
</task>

</tasks>

<must_haves>
  truths:
    - "User can cancel an order and stock is restored"
    - "Integration test verifies concurrent stock modification"
  artifacts:
    - path: "src/test/java/com/shop/OrderIntegrationTest.java"
      provides: "Checkout concurrency and cancellation verification"
</must_haves>

<success_criteria>
Checkout phase is complete, thoroughly tested, and handles stock concurrency safely.
</success_criteria>

<output>
After completion, create `.planning/phases/04-checkout-order-management/04-03-SUMMARY.md`
</output>