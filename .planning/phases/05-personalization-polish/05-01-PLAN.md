---
phase: 05-personalization-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pom.xml
  - src/main/resources/db/migration/V5__create_recommendation_schema.sql
  - src/main/java/com/shop/domain/recommendation/entity/RecentView.java
  - src/main/java/com/shop/domain/recommendation/repository/RecentViewRepository.java
  - src/main/java/com/shop/global/config/SwaggerConfig.java
  - src/main/java/com/shop/global/config/SecurityConfig.java
autonomous: true
requirements:
  - REC-01
must_haves:
  truths:
    - Application starts with Swagger UI enabled at /swagger-ui.html
    - Swagger UI allows JWT Bearer token authorization
    - Database schema has recent_views table with UUID v7 PK
  artifacts:
    - path: src/main/resources/db/migration/V5__create_recommendation_schema.sql
      provides: Database schema for recent_views
    - path: src/main/java/com/shop/domain/recommendation/entity/RecentView.java
      provides: Entity mapping for recent_views
    - path: src/main/java/com/shop/global/config/SwaggerConfig.java
      provides: OpenAPI configuration
  key_links:
    - from: SwaggerConfig
      to: SecurityConfig
      via: PermitAll for Swagger paths
---

<objective>
Setup the infrastructure for Phase 5, including Open API documentation, caching dependencies, and the `recent_views` database schema.

Purpose: Provide the foundational tools (Swagger, Spring Cache, Hypersistence test utility) and the core entity for personalization.
Output: Updated `pom.xml`, new Flyway migration, `RecentView` entity, and fully configured Swagger UI with JWT support.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-personalization-polish/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Dependencies</name>
  <files>pom.xml</files>
  <action>
    Add the following dependencies to the `pom.xml`:
    1. `springdoc-openapi-starter-webmvc-ui` (version `2.8.5` or compatible) for OpenAPI 3 support.
    2. `spring-boot-starter-cache` for `@Cacheable` support.
    3. `io.hypersistence:hypersistence-utils-hibernate-63` (version `3.8.2`, scope `test`) to detect N+1 issues in integration tests.
    Ensure `uuid-creator` is still present (it should be).
  </action>
  <verify>
    <automated>mvn dependency:resolve | findstr "springdoc-openapi"</automated>
  </verify>
  <done>Dependencies added without breaking the build</done>
</task>

<task type="auto">
  <name>Task 2: Create Recommendation Schema & Entity</name>
  <files>
    src/main/resources/db/migration/V5__create_recommendation_schema.sql
    src/main/java/com/shop/domain/recommendation/entity/RecentView.java
    src/main/java/com/shop/domain/recommendation/repository/RecentViewRepository.java
  </files>
  <action>
    Create Flyway script `V5__create_recommendation_schema.sql` to add the `recent_views` table:
    - `id`: UUID (Primary Key, will use UUID v7).
    - `user_id`: UUID (NOT NULL, FK to users).
    - `product_id`: UUID (NOT NULL, FK to products).
    - `viewed_at`: TIMESTAMP (NOT NULL, DEFAULT NOW()).
    Add an index on `(user_id, viewed_at DESC)` for efficient querying.
    
    Create `RecentView` entity mapping to this table:
    - Generate `id` using `UuidCreator.getTimeOrderedEpoch()`.
    - Map `User` (Lazy) and `Product` (Lazy).
    - Set `viewed_at` on `@PrePersist`.
    
    Create a basic `RecentViewRepository` extending `JpaRepository`.
  </action>
  <verify>
    <automated>mvn flyway:info -Dflyway.url=jdbc:postgresql://localhost:5432/shop_db_test -Dflyway.user=shop_user -Dflyway.password=shop_pass</automated>
  </verify>
  <done>Database migrates successfully and entity compiles</done>
</task>

<task type="auto">
  <name>Task 3: Configure Swagger UI with JWT Support</name>
  <files>
    src/main/java/com/shop/global/config/SwaggerConfig.java
    src/main/java/com/shop/global/config/SecurityConfig.java
  </files>
  <action>
    Create `SwaggerConfig` annotated with `@Configuration`.
    Add OpenAPI Bean configuring:
    - Title: Shop-Back API
    - Version: v1
    - Security Scheme: HTTP Bearer, format JWT.
    Apply the security requirement globally so all endpoints show the lock icon.
    
    Update `SecurityConfig`:
    - Allow unauthenticated access (`permitAll`) to `/v3/api-docs/**`, `/swagger-ui/**`, and `/swagger-ui.html`.
  </action>
  <verify>
    <automated>mvn clean compile</automated>
  </verify>
  <done>Swagger config is complete and SecurityConfig permits the documentation paths</done>
</task>

</tasks>

<verification>
Check that `mvn clean test` compiles. (No tests to run for this plan specifically, but ensuring no compilation failure).
</verification>

<success_criteria>
- Swagger UI is fully configured.
- The `recent_views` table is ready via Flyway.
- Required performance and documentation dependencies are resolved.
</success_criteria>

<output>
After completion, create `.planning/phases/05-personalization-polish/05-01-SUMMARY.md`
</output>
