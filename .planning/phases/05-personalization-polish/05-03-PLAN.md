---
phase: 05-personalization-polish
plan: 03
type: execute
wave: 3
depends_on:
  - 02
files_modified:
  - src/main/java/com/shop/domain/cart/repository/CartRepository.java
  - src/main/java/com/shop/domain/order/repository/OrderRepository.java
  - src/test/java/com/shop/RecommendationIntegrationTest.java
autonomous: true
requirements:
  - REC-01
must_haves:
  truths:
    - Cart and Order fetching do not produce N+1 queries
    - Integration tests prove recommendations work as expected
    - Integration tests prove cache hits reduce query counts
  artifacts:
    - path: src/test/java/com/shop/RecommendationIntegrationTest.java
      provides: Comprehensive testing for Phase 5 features
  key_links:
    - from: RecommendationIntegrationTest
      to: RecommendationController
      via: MockMvc tests
---

<objective>
Finalize Phase 5 by eliminating N+1 query problems in existing entities and validating the entire Personalization & Polish implementation via integration tests.

Purpose: Guarantee performance under load by removing accidental JPA lazy loading side effects and ensure recommendations are accurate.
Output: Optimized JPA repositories and a new, comprehensive integration test suite for the Recommendation domain.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-personalization-polish/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eliminate N+1 Issues</name>
  <files>
    src/main/java/com/shop/domain/cart/repository/CartRepository.java
    src/main/java/com/shop/domain/order/repository/OrderRepository.java
  </files>
  <action>
    Review existing endpoints for `Cart` and `Order` retrieval.
    Where lazy-loaded collections (`items`, `items.product`) are accessed:
    1. In `CartRepository`, add `@EntityGraph(attributePaths = {"items", "items.product"})` to `findByUserId()` or equivalent retrieval methods.
    2. In `OrderRepository`, add `@EntityGraph(attributePaths = {"items", "items.product"})` to `findByIdWithItems()` or `findAllByUserId()`.
    Ensure we don't fetch-join multiple collections simultaneously to avoid MultipleBagFetchException (Cart and Order only have one `items` collection each, so this should be fine).
  </action>
  <verify>
    <automated>mvn clean compile</automated>
  </verify>
  <done>N+1 vulnerabilities are patched using EntityGraphs</done>
</task>

<task type="auto">
  <name>Task 2: Write Recommendation & Performance Tests</name>
  <files>
    src/test/java/com/shop/RecommendationIntegrationTest.java
  </files>
  <action>
    Create `RecommendationIntegrationTest` using `@SpringBootTest` and `@AutoConfigureMockMvc`.
    Use `SQLStatementCountValidator` (from `hypersistence-utils`) to assert query counts.
    
    Write tests for:
    1. `testRecordView()`: Posts to `/api/products/{id}/view` and asserts `RecentView` is saved.
    2. `testGetRecommendations()`: Verifies hybrid recommendation logic returns expected mock products based on recent views/orders.
    3. `testCategoryCache()`: Fetches the category tree twice, asserting the second call executes 0 SQL queries (verifying `@Cacheable`).
    4. `testSwaggerUiAccess()`: Performs a GET request to `/v3/api-docs` or `/swagger-ui/index.html` to ensure they return HTTP 200 without authentication.
  </action>
  <verify>
    <automated>mvn test -Dtest=RecommendationIntegrationTest</automated>
  </verify>
  <done>All integration tests for Phase 5 pass</done>
</task>

</tasks>

<verification>
Check the output of `mvn test` to ensure no regressions have been introduced across the application, especially with the JPA repository changes.
</verification>

<success_criteria>
- Zero N+1 queries during standard Cart and Order fetches.
- Integration test proves that `@Cacheable` prevents database queries on subsequent calls.
- Swagger UI path is accessible and verified via tests.
- Recommendations yield correct output format.
</success_criteria>

<output>
After completion, create `.planning/phases/05-personalization-polish/05-03-SUMMARY.md`
</output>
