---
phase: 03-shopping-cart-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "src/main/resources/db/migration/V3__create_cart_schema.sql",
  "src/main/java/com/shop/domain/cart/Cart.java",
  "src/main/java/com/shop/domain/cart/CartItem.java",
  "src/main/java/com/shop/domain/cart/CartIdentifier.java",
  "src/main/java/com/shop/domain/cart/CartRepository.java",
  "src/main/java/com/shop/domain/cart/CartItemRepository.java",
  "src/main/java/com/shop/global/config/ShedLockConfig.java"
]
autonomous: true
requirements: [CART-01]
---

<objective>
Establish the shopping cart data foundation by creating the database schema, entity mappings, and basic repositories.

Purpose: Provide persistent storage for both member and guest shopping carts.
Output: Flyway V3 migration, Cart/CartItem entities, JPA repositories, and ShedLock configuration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-shopping-cart-persistence/03-RESEARCH.md
@src/main/java/com/shop/domain/catalog/Product.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flyway V3 Migration (Carts & ShedLock)</name>
  <files>src/main/resources/db/migration/V3__create_cart_schema.sql</files>
  <action>
    Create a new Flyway migration script defining the cart tables and ShedLock table:
    - `carts` table: `id` (UUID, PK), `user_id` (UUID, Nullable, Index), `guest_cart_id` (UUID, Nullable, Index), `created_at`, `updated_at`.
    - `cart_items` table: `id` (UUID, PK), `cart_id` (UUID, FK to `carts.id`), `product_id` (UUID, FK to `products.id`), `quantity` (INT), `created_at`, `updated_at`. Unique constraint on `(cart_id, product_id)`.
    - `shedlock` table: `name` (VARCHAR(64), PK), `lock_until` (TIMESTAMP), `locked_at` (TIMESTAMP), `locked_by` (VARCHAR(255)).
    Ensure all IDs are compatible with UUID v7 (standard UUID type in PostgreSQL).
  </action>
  <verify>
    File exists and contains table definitions for `carts`, `cart_items`, and `shedlock`.
  </verify>
  <done>Flyway migration file created and schema defined.</done>
</task>

<task type="auto">
  <name>Task 2: Cart & CartItem Entity Implementation</name>
  <files>
    src/main/java/com/shop/domain/cart/Cart.java,
    src/main/java/com/shop/domain/cart/CartItem.java,
    src/main/java/com/shop/domain/cart/CartIdentifier.java
  </files>
  <action>
    Implement the cart domain models using JPA:
    - `Cart`: Entity with `user_id`, `guest_cart_id`, and `List<CartItem> items`. Use `@PrePersist` for `UuidCreator.getTimeOrderedEpoch()`.
    - `CartItem`: Entity with reference to `Cart` and `Product` (from catalog domain), and `quantity`. Use UUID v7 for PK.
    - `CartIdentifier`: A DTO or Record to wrap `userId` and `guestCartId` for unified identification in service layer.
    Include standard auditing fields (`createdAt`, `updatedAt`).
  </action>
  <verify>
    Entities created with correct JPA annotations and UUID v7 generation logic.
  </verify>
  <done>Entities and identifier DTO implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Repositories and ShedLock Config</name>
  <files>
    src/main/java/com/shop/domain/cart/CartRepository.java,
    src/main/java/com/shop/domain/cart/CartItemRepository.java,
    src/main/java/com/shop/global/config/ShedLockConfig.java
  </files>
  <action>
    - Create `CartRepository` and `CartItemRepository` interfaces extending `JpaRepository`.
    - Configure ShedLock in `ShedLockConfig.java` as per RESEARCH.md (using `JdbcTemplateLockProvider`).
    - Add required dependencies for ShedLock to `pom.xml` if not already present (`shedlock-spring`, `shedlock-provider-jdbc-template`).
  </action>
  <verify>
    - Repositories exist.
    - `ShedLockConfig` uses `JdbcTemplateLockProvider` and `dataSource`.
    - `pom.xml` contains ShedLock dependencies.
  </verify>
  <done>Repositories and scheduling infrastructure configured.</done>
</task>

</tasks>

<must_haves>
  truths:
    - "Database tables for carts and shedlock are defined"
    - "Entities support member and guest identification"
    - "UUID v7 is used for all primary keys"
  artifacts:
    - path: "src/main/resources/db/migration/V3__create_cart_schema.sql"
      provides: "Cart and ShedLock schema"
    - path: "src/main/java/com/shop/domain/cart/Cart.java"
      provides: "Cart persistence model"
    - path: "src/main/java/com/shop/global/config/ShedLockConfig.java"
      provides: "Distributed locking configuration"
  key_links:
    - from: "src/main/java/com/shop/domain/cart/CartItem.java"
      to: "src/main/java/com/shop/domain/catalog/Product.java"
      via: "JPA ManyToOne relationship"
</must_haves>

<success_criteria>
Cart data model is fully implemented and scheduling infrastructure is ready for batch tasks.
</success_criteria>

<output>
After completion, create `.planning/phases/03-shopping-cart-persistence/03-01-SUMMARY.md`
</output>
