---
phase: 03-shopping-cart-persistence
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [
  "src/main/java/com/shop/domain/cart/PostgresCartService.java",
  "src/main/java/com/shop/domain/cart/CartController.java",
  "src/main/java/com/shop/domain/cart/CartCleanupBatch.java",
  "src/test/java/com/shop/CartIntegrationTest.java"
]
autonomous: true
requirements: [CART-01, CART-02]
---

<objective>
Complete the shopping cart phase by implementing merge logic, scheduled cleanup, and comprehensive integration tests.

Purpose: Finalize cart lifecycle management and ensure system reliability.
Output: Cart merging API, @Scheduled cleanup task with ShedLock, and CartIntegrationTest.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-shopping-cart-persistence/03-RESEARCH.md
@.planning/phases/03-shopping-cart-persistence/03-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cart Merging Implementation</name>
  <files>
    src/main/java/com/shop/domain/cart/PostgresCartService.java,
    src/main/java/com/shop/domain/cart/CartController.java
  </files>
  <action>
    Implement logic to merge a guest cart into a user cart:
    - `PostgresCartService.mergeCart(UUID userId, UUID guestCartId)`:
        - Retrieve both carts.
        - Merge items: combine quantities for existing products (obeying limits), move new products.
        - Delete guest cart after merge.
    - `CartController.mergeCart(UUID guestCartId)`: 
        - Endpoint `POST /api/cart/merge`.
        - Requires authenticated user context.
  </action>
  <verify>
    - Merging logic handles quantity limits.
    - Guest cart is deleted after successful merge.
  </verify>
  <done>Cart merging functionality implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Scheduled Cart Cleanup with ShedLock</name>
  <files>src/main/java/com/shop/domain/cart/CartCleanupBatch.java</files>
  <action>
    Implement a scheduled batch job to delete abandoned carts:
    - Use `@Scheduled` with a cron expression for daily execution.
    - Use `@SchedulerLock` from ShedLock to prevent concurrent execution.
    - Logic: Delete `Cart` entries (and cascading `CartItem`s) where `updated_at` is older than 7 days and `user_id` is null (guest carts) OR all carts as per requirement.
  </action>
  <verify>
    Scheduler is configured and deletion logic correctly targets old data.
  </verify>
  <done>Automated data lifecycle management implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Cart Integration Testing</name>
  <files>src/test/java/com/shop/CartIntegrationTest.java</files>
  <action>
    Write comprehensive integration tests covering:
    - Adding items as Guest and User.
    - Retrieving cart with correct product details and subtotal.
    - Enforcing quantity limits (99 per item, 50 unique items).
    - Merging guest cart into member account.
    - Verifying stock validation logic during item addition.
  </action>
  <verify>
    Run `./mvnw test -Dtest=CartIntegrationTest` and all tests pass.
  </verify>
  <done>Phase 3 functionality verified with automated tests.</done>
</task>

</tasks>

<must_haves>
  truths:
    - "Guest items are successfully merged into user accounts"
    - "Inactive guest carts are automatically deleted after 7 days"
    - "All cart operations are verified via automated integration tests"
  artifacts:
    - path: "src/main/java/com/shop/domain/cart/CartCleanupBatch.java"
      provides: "Automated cleanup task"
    - path: "src/test/java/com/shop/CartIntegrationTest.java"
      provides: "Functional verification suite"
  key_links:
    - from: "src/main/java/com/shop/domain/cart/CartCleanupBatch.java"
      to: "shedlock table"
      via: "@SchedulerLock annotation"
</must_haves>

<success_criteria>
Shopping cart system is robust, self-cleaning, and fully verified for all user scenarios.
</success_criteria>

<output>
After completion, create `.planning/phases/03-shopping-cart-persistence/03-03-SUMMARY.md`
</output>
